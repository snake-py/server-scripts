DOMAIN=example.com

# Full database URL — URL-encode any special chars in the password
# e.g. : → %3A, # → %23, @ → %40, } → %7D, ^ → %5E, ) → %29
DATABASE_URL=postgres://windmill:changeme@db/windmill?sslmode=disable
